def get_integer(m):
    my_integer = int(input(m))
    return my_integer

def get_string(m):
    my_string = input(m)
    return my_string
    print("."*100)


def add_names_age(N,A):
    run = True
    while run == True:
        name = get_string("Please enter a name:")
        my_string = input(m)
        return my_string
        age = get_string("Please enter {}'s age".format(name))
        my_string = input(m)
        return my_string       
        N.append(name)
        A.append(age)
        run = False
    

def menu():
    # creating the empty lists
    # the pair values are parallel lists
    name = []
    age = []
    
